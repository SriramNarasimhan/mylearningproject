function check(){
	try{

		var path = workflowData.getPayload().toString();
		var jcrsession = workflowSession.getSession();
		var node = jcrsession.getNode(path).getNode("jcr:content");
		if(null != node && node.hasProperty("compliancereviewer")) {
			if(node.getProperty("compliancereviewer").getValue() != "" && node.getProperty("compliancereviewer").getValue() != null){
				return false;
			}else{
				return true;
			}
		}else{		
			return true;
		}
	}catch(err) {
	 log.info(err.message);
	 return false;
	}
}