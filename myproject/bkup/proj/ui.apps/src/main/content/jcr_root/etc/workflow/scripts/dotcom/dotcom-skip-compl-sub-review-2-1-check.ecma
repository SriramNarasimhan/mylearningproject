function check(){
try{
	var path = workflowData.getPayload().toString();
	var jcrsession = workflowSession.getSession();
	var node = jcrsession.getNode(path).getNode("jcr:content");
    if(null != node && node.hasProperty("21compliancereviewer") && node.getProperty("21compliancereviewer").getValue() != "" && node.getProperty("21compliancereviewer").getValue() != null){
		return false;
	}else{		
        return true;
    }
}catch(err) {
 log.info(err.message);
 return false;
}
}