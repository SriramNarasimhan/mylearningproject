function check(){
try{
	var path = workflowData.getPayload().toString();
	var jcrsession = workflowSession.getSession();
	var node = jcrsession.getNode(path).getNode("jcr:content");
    if(null != node && node.hasProperty("skipqareview")){
		workflowData.getMetaDataMap().put("skipqareview", node.getProperty("skipqareview").getValue());
		return true;
	}else{		
        return false;
    }
}catch(err) {
 log.info(err.message);
 return false;
}
}